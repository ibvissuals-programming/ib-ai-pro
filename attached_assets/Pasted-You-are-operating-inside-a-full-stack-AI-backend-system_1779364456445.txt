You are operating inside a full-stack AI backend system. Apply all changes directly in the codebase with zero external dependencies and minimal cost. Implement system hardening across contract validation authentication safety and job pipeline integrity. On server startup run silent integrity checks that load all users from PostgreSQL and ensure the CEO account exists exactly once ensure no duplicate usernames exist ensure getUserByUsername always returns the correct user and verify that in-memory indexes such as usernameIndex and idIndex are fully synchronized with PostgreSQL after load auto-repairing any missing or inconsistent index entries during startup. Also verify provider readiness using isGeminiConfigured and if it returns false immediately enable SAFE MODE which blocks all job creation and AI processing. At runtime before any image video or TTS job is created enforce strict guards that require valid authentication valid session or user context and confirmed provider availability and if any condition fails do not create the job and return a structured error immediately without queueing or fallback execution. Enforce system invariants globally so that no job can ever be marked completed without a real output artifact no provider failure can ever return a success response no silent fallback responses are allowed anywhere in the system and no user creation can bypass index synchronization and log all invariant violations as SYSTEM_INVARIANT_VIOLATION. Implement a password rotation endpoint POST /api/auth/change-password that requires authentication requires current password verification uses the existing password hashing system only updates both PostgreSQL and in-memory user store invalidates any existing session if supported logs password_changed events enforces minimum password length of six characters prevents empty passwords prevents reuse of current password and works for all users including CEO. Replace all AI job handling with a strict state machine using only the states created validated queued processing completed and failed and enforce all transitions exclusively through a transitionJob function that rejects invalid transitions logs invalid attempts and prevents direct state mutation. All AI services including image video and TTS must follow the full lifecycle created to validated to queued to processing to completed or failed with no skipped states allowed. All failures must always transition jobs to failed state must include errorCode must include provider information when relevant must never generate fake outputs and must never mark success on failure. Apply global cost control rules that forbid external libraries forbid new infrastructure services enforce fail-fast behavior before job creation prefer in-memory validation over database calls when safe avoid redundant checks and eliminate duplicate logic paths. Final system behavior must guarantee CEO authentication survives restarts without duplication guarantees no index mismatch between database and memory guarantees no fake job completion under any condition guarantees provider failures are explicit and safe guarantees all jobs follow strict lifecycle enforcement guarantees password changes work safely across database and memory layers and guarantees deterministic predictable system behavior across all services.