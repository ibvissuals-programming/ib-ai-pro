IB AI — IMPORT STABILIZATION + SECRET PERSISTENCE HARDENING (CREDIT-EFFICIENT SINGLE PASS)

MISSION:
Eliminate repeated Replit import/setup friction and make every future GitHub → Replit migration deterministic, low-credit, and near-zero manual setup.

CRITICAL RULE:
DO NOT rebuild existing systems.
DO NOT rescan the entire codebase.
DO NOT rewrite stable services.
ONLY patch the import/setup lifecycle.

---------------------------------------------------
PHASE 1 — SECRET DEPENDENCY REDUCTION
---------------------------------------------------

Audit all environment variable usage.

GOAL:
Reduce required secrets to the minimum possible.

RULES:
- GEMINI_API_KEY = ONLY required AI provider secret
- Automatically generate fallback values for:
  - JWT_SECRET
  - CEO_RECOVERY_KEY
  - SESSION_SECRET
- If CEO_PASSWORD is missing:
  - auto-create temporary bootstrap password
  - print it once in backend logs
  - mark account for forced reset later

DO NOT block startup for missing optional secrets.

OPTIONAL SECRETS:
- GROQ_API_KEY
- VIDEO_ENABLED
- VEO_MODEL
- REDIS_URL

If optional secrets are missing:
- system starts normally
- affected features become gracefully disabled
- health endpoint reports feature_disabled

---------------------------------------------------
PHASE 2 — IMPORT AUTO-DETECTION
---------------------------------------------------

Create startup detection logic:

IF:
- running inside fresh Replit import
- node_modules missing
- DB already exists
- workflows duplicated
- schema already deployed

THEN:
- skip unnecessary reinstall loops
- skip schema push
- skip duplicate workflow restart
- skip redundant environment setup

Add:
scripts/import-bootstrap.cjs

Responsibilities:
1. detect fresh import
2. verify package state
3. verify DB state
4. verify required secrets
5. print concise system report
6. STOP

NO recursive agent loops.
NO repeated scans.

---------------------------------------------------
PHASE 3 — GEMINI-FIRST PROVIDER MODE
---------------------------------------------------

Make Gemini the canonical provider.

RULES:
- all systems work with ONLY GEMINI_API_KEY
- no Groq dependency required
- no fallback errors when Groq missing
- all health systems reflect Gemini-primary mode

If Groq missing:
- routing automatically bypasses Groq
- no warning spam
- no degraded startup state

---------------------------------------------------
PHASE 4 — CEO AUTO-BOOTSTRAP
---------------------------------------------------

If no CEO account exists:

Automatically:
- create CEO account
- use env password OR generated temporary password
- log:
  [bootstrap] CEO account initialized

If CEO already exists:
- skip silently
- NEVER recreate
- NEVER overwrite

---------------------------------------------------
PHASE 5 — STARTUP HARDENING
---------------------------------------------------

Create:
scripts/startup-health.cjs

On every backend boot:

Verify:
- DB connectivity
- Gemini connectivity
- workflow state
- required directories
- storage permissions
- route registration

Output:
✔ DB
✔ Gemini
✔ Storage
✔ Routes
✔ AI Services

OR:
✗ clear failure reason

Startup checks must complete in under 3 seconds.

---------------------------------------------------
PHASE 6 — HEALTH + IMPORT VISIBILITY
---------------------------------------------------

Expand:
GET /api/ai/system-health

Add:
- importReady
- bootstrapComplete
- missingOptionalServices
- providerMode
- setupWarnings

Expected result example:

{
  "status": "healthy",
  "providerMode": "gemini-primary",
  "importReady": true,
  "bootstrapComplete": true,
  "missingOptionalServices": ["groq", "veo"],
  "setupWarnings": []
}

---------------------------------------------------
PHASE 7 — REPLIT IMPORT LAW
---------------------------------------------------

Update replit.md permanently.

Rules:
- NEVER request already optional secrets
- NEVER push schema if tables exist
- NEVER reinstall packages if node_modules valid
- NEVER duplicate workflow restarts
- NEVER block startup for missing optional AI providers
- ALWAYS prefer Gemini-primary execution mode
- ALWAYS complete import validation before agent rebuild attempts

---------------------------------------------------
PHASE 8 — VALIDATION
---------------------------------------------------

Run:

pnpm run guard
pnpm run health

Then verify:

✔ Import works with ONLY GEMINI_API_KEY
✔ Existing DB preserved
✔ CEO account preserved
✔ AI routes operational
✔ Health endpoint healthy
✔ No setup loops
✔ No duplicate workflow rebuilds
✔ No repeated secret prompts

---------------------------------------------------
FINAL RULES
---------------------------------------------------

- Minimize credit consumption aggressively
- Reuse existing infrastructure only
- Avoid broad code scans
- Touch only import/setup lifecycle files
- Preserve all existing multimodal systems
- Preserve UI stability architecture
- Preserve health monitoring system
- Preserve Glass Monolith branding system

OUTPUT:
- files modified
- startup flow summary
- import flow summary
- remaining optional services
- final readiness score